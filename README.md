# sl4jDemo
Steps :
1. clone the project
2. `mvn clean install` to compile and intall the project
3. Above step creates executable jar in the target folder
4. use following command to run `java -Dlog4j.configuration=file:"log4j_property_file_path" -jar SL4JTest-0.0.1-SNAPSHOT.jar`

